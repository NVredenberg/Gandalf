# Templates

Dieser Ordner bleibt bewusst leer.

Die DOCX-Ausgabe verwendet keine KI-generierten Templates. Die verbindliche Tabellenstruktur wird in `backend/renderer/structure.js` und `backend/renderer/docxRenderer.js` durch Code erzeugt.
